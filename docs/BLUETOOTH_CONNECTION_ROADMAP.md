# 蓝牙连接完善计划

## 目标

把当前经典蓝牙 SPP 功能从“能够连接和收发”提升为可诊断、可恢复、可测试的稳定连接系统，同时保持现有主页、服务、历史、调试和聊天功能可用。

## 基线与原则

- 继续使用经典蓝牙 RFCOMM/SPP，不在本计划中混入 BLE。
- 客户端连接与服务端接入共用同一种会话生命周期。
- 同一 MAC 地址只保留一个有效会话；连接中禁止重复发起。
- 连接层不直接控制页面，统一发布状态，页面只负责展示和用户操作。
- 每个阶段必须通过单元测试、`assembleDebug`，涉及设备行为时再通过真机测试。

## 阶段 1：连接生命周期和资源安全（进行中）

### 工作项

- [x] 建立统一连接状态：空闲、配对中、连接中、已连接、重连中、断开中、已断开、失败。
- [x] 增加按 MAC 地址管理的连接注册表，原子阻止重复连接。
- [ ] 客户端支持取消正在进行的连接，并保证超时后 Socket 被关闭。（连接层取消入口已完成，待接入界面按钮）
- [x] 客户端连接前停止设备扫描。
- [ ] 服务端和客户端会话统一登记、替换和关闭规则。
- [x] 应用销毁时关闭全部活动会话并清空运行时状态。
- [ ] 统一断开通知，确保一次断开只产生一次业务事件。
- [x] 修复广播中无条件读取 RSSI、空设备对象和 switch 贯穿风险。

### 验收标准

- 连续快速点击同一设备只产生一次连接尝试。
- 15 秒超时或用户取消后，不保留连接线程或 Socket。
- 相同设备不会同时存在两个活动会话。
- 应用退出后服务端 Socket、客户端 Socket、接收线程全部关闭。
- 连接状态转换有单元测试覆盖。

## 阶段 2：配对、权限和扫描（待执行）

- [ ] 未配对设备先发起 `createBond()`，配对成功后自动继续连接。
- [ ] 展示配对中、配对拒绝、配对失败，并允许重新配对。
- [ ] Android 6–11 与 Android 12+ 权限分别处理，拒绝后支持重新申请和跳转设置。
- [ ] 监听蓝牙开关变化；关闭时终止任务，重新开启后恢复服务。
- [ ] 监听扫描开始和结束，增加扫描超时、空结果和失败状态。
- [ ] 已配对设备与附近设备分组，按 RSSI/名称排序并去重。

### 验收标准

- Android 8、11、12、14/15 权限路径均有明确结果。
- 未配对设备可以在一次用户流程内完成配对并连接。
- 蓝牙关闭或权限撤销不会导致崩溃。

## 阶段 3：自动重连和后台连接（待执行）

- [ ] 保存每台设备的自动重连开关和最近成功参数。
- [ ] 异常断开采用 1、2、5、10、30 秒退避重试，设置最大次数。
- [ ] 用户主动断开时不触发自动重连。
- [ ] 网络式抖动场景下只保留一个重连任务。
- [ ] 使用前台服务承载后台连接，并提供连接状态通知和断开入口。
- [ ] 系统杀进程后的恢复策略明确且可配置。

### 验收标准

- 远端设备短暂掉电后恢复，可在配置范围内自动连回。
- 主动断开、退出应用或关闭自动重连时不会偷偷重连。

## 阶段 4：协议和收发可靠性（待执行）

- [ ] 每个连接使用独立串行发送队列，避免并发写流。
- [ ] 增加发送状态：排队、成功、失败、取消。
- [ ] 支持原始流、CRLF、LF、CR、固定长度、自定义结束符和超时分包。
- [ ] 帧缓存设置上限，超限时丢弃或切帧并记录诊断事件。
- [ ] 支持 UTF-8、GBK、ASCII、HEX 展示与发送。
- [ ] 大数据发送支持分片、间隔、进度、取消和失败终止。
- [ ] 明确空帧、半包、粘包、尾部残帧的处理规则。

### 验收标准

- 10 MB 无结束符输入不会无限占用内存。
- 多线程同时发送时，字节不会交叉。
- 分包、粘包、中文编码、二进制零字节均有自动化测试。

## 阶段 5：设备和会话体验（待执行）

- [ ] 已连接设备、连接中设备、历史设备统一展示。
- [ ] 支持设备别名、收藏、最近连接、单设备 UUID/协议配置。
- [ ] 点击已连接设备直接进入会话，不重复连接。
- [ ] 多设备接入时进入连接列表或通知，不由后台直接拉起聊天页面。
- [ ] 展示连接时长、收发字节、帧数、速率和错误数。

## 阶段 6：诊断、日志和测试（待执行）

- [ ] 错误分类：权限、适配器关闭、配对失败、UUID 无效、超时、远端拒绝、读写失败。
- [ ] 结构化连接日志包含时间、MAC、状态转换、线程和异常摘要。
- [ ] 导出文本/HEX 日志时包含方向、时间和设备信息。
- [ ] 为状态机、分帧器、退避策略和发送队列增加单元测试。
- [ ] 使用两台 Android 设备或 Android + SPP 模块验证客户端/服务端、多连接、掉线恢复。
- [ ] 建立回归清单：扫描、配对、连接、收发、切后台、旋转、断开、重连、退出。

## 执行顺序

严格按阶段 1 → 2 → 3 → 4 → 5 → 6 推进。每完成一个阶段：

1. 更新本文件复选框和验证记录。
2. 运行 `./gradlew testDebugUnitTest`。
3. 运行 `./gradlew assembleDebug`。
4. 涉及系统蓝牙行为时补充真机验证结果。

## 验证记录

| 日期 | 阶段 | 命令/设备 | 结果 |
|---|---|---|---|
| 2026-06-30 | 计划建立 | 代码审查 | 已识别连接生命周期、配对、重连、协议和广播处理缺口 |
| 2026-06-30 | 阶段 1（首批） | `testDebugUnitTest assembleDebug` | 本机 SDK 目录在当前执行环境中不可访问；Gradle 长时间无输出后终止，待环境恢复后重跑 |
